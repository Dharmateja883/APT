from fastapi import FastAPI # pyright: ignore[reportMissingImports]

app = FastAPI()

@app.get("/")
def home():
    return {"message": "Hello World"}

@app.get("/user/{id}")
def get_user(id: int):
    return {"user_id": id}